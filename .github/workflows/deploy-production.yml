name: Deploy Production

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./infrastructure/docker/Dockerfile.prod
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        with:
          host: ${{ secrets.DEVFLOWFIX_HOST }}
          username: ${{ secrets.DEVFLOWFIX_USER }}
          key: ${{ secrets.DEVFLOWFIX_SSH_KEY }}
          envs: IMAGE_NAME
          script: |
            set -e
            cd "${{ secrets.DEVFLOWFIX_APP_PATH }}"
            test -f devflowfix.env || { echo "Missing devflowfix.env on VPS"; exit 1; }
            test -f compose.prod.yml || { echo "Missing compose.prod.yml on VPS"; exit 1; }
            docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" -p "${{ secrets.GHCR_TOKEN }}"
            export BACKEND_IMAGE="${IMAGE_NAME}:latest"
            docker compose --env-file devflowfix.env -f compose.prod.yml pull devflowfix-backend
            docker compose --env-file devflowfix.env -f compose.prod.yml up -d --no-deps devflowfix-backend
            docker image prune -f
